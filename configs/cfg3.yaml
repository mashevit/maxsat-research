ea:
  enabled: true
  pop_size: 110
  elitism: True
ls:
  ls_polish_flips: 5
  time_limit_s: 0.001
  flip_budget: 0
io:
  out_json: "runs/{stem}11.ea.json"   # where to write results
  quiet: false      
  
time_limit_s: 0.05              # suppress human summary on stderr if true